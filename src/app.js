"use strict";
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-1") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-2") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-3") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-4") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-5") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
window.addEventListener("scroll", () => {
    const listNode = document.querySelectorAll(".tecnologies__card-6") || document.createElement("div");
    const firstElement = listNode[0];
    const position = firstElement.getBoundingClientRect().top;
    const tamanioPantalla = window.innerHeight / 2;
    for (let i = 0; i < listNode.length; i++) {
        let element = listNode[i];
        if (position < tamanioPantalla) {
            element.classList.add("tecnologies__fade_in");
            element.classList.remove("tecnologies__fade_out");
        }
        else {
            element.classList.remove("tecnologies__fade_in");
            element.classList.add("tecnologies__fade_out");
        }
    }
    ;
});
